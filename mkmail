echo "Don't use this version. A secured version is on the way"
# sudo debconf-set-selections <<< 'mysql-server mysql-server/root_password password password443'
# sudo debconf-set-selections <<< 'mysql-server mysql-server/root_password_again password password443'
# sudo apt-get -y install mysql-server

